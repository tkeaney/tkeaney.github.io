# tkeaney.github.io

Project setup & data model design
 Core task UI — add, edit, complete, delete
 AI-powered task sorting via Claude API
 User context layer — the app starts learning preferences
 Feedback loop — behavior shapes future prioritization


Running Locally
bashgit clone https://github.com/your-username/smartstack.git
cd smartstack
npm install
npm run dev

You'll need an Anthropic API key once the AI layer is wired in. Add it to a .env file — see .env.example.


About
Built by Tess — a developer interested in the space where useful software meets thoughtful AI.

This project is intentionally open-ended. The best version of it probably doesn't exist yet.